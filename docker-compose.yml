services:
  # --- THE DATABASE ---
  mongo_db:
    image: mongo:latest
    container_name: store-db
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  # --- THE MESSAGE BROKER (KAFKA) ---
  # zookeeper:
  #   image: bitnami/zookeeper:latest
  #   environment:
  #     - ALLOW_ANONYMOUS_LOGIN=yes

  # kafka:
  #   image: bitnami/kafka:latest
  #   ports:
  #     - "9092:9092"
  #   environment:
  #     - KAFKA_CFG_ZOOKEEPER_CONNECT=zookeeper:2181
  #     - ALLOW_PLAINTEXT_LISTENER=yes
  #     - KAFKA_CFG_ADVERTISED_LISTENERS=PLAINTEXT://localhost:9092
  #   depends_on:
  #     - zookeeper

  # --- MICROSERVICES ---
  
  # Node.js Service (The Frontend API)
  node-api:
    build: ./backend
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo_db:27017/shop
      # - KAFKA_BROKER=kafka:9092
    depends_on:
      - mongo_db
      # - kafka

  # # Go Service (The Inventory/Shipping Processor)
  # go-worker:
  #   build: ./go-app
  #   environment:
  #     - KAFKA_BROKER=kafka:9092
  #   depends_on:
  #     - kafka

volumes:
  mongo_data:

# To use mongo like psql:
# docker exec -it store-db mongosh  
# docker logs -f store-db